本章目标字数：{target_chapter_words} 字

### 章节大纲
{outline}

### 你的任务
将上述章纲拆成 **专业节拍执行计划**，节拍数量参考：
- {target_chapter_words} 字 → 建议 {target_chapter_words} ÷ 400 ≈ 约 N 个节拍（向上取整）

### 自检清单（输出前必须过一遍）
1. 节拍数量是否合理（每拍约 300-500 字）？
2. 每个 intent 是否描述了**可感知的叙事推进**（不是静态描写）？
3. `focus` 类型是否多样，避免连续 3 拍都是同一类型？
4. `transition_hint` 是否指出了具体的过渡方式，而不是空泛描述？

直接输出 JSON，不加任何前置说明或 Markdown 围栏。
